# Docker environment variables
# Copy this file to .env and adjust as needed

VITE_WS_URL=ws://localhost:3001

# Debug-only: let the server send word coordinates so the client can highlight
# them. NEVER enable in production — it leaks word locations. Pair with
# VITE_DEBUG_WORDS=true on the client.
# DEBUG_WORDS=true
